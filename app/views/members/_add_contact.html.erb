<%= form_for(current_member.relationships.build(contact_id: @member.id)) do |f| %>
    <div><%= f.hidden_field :contact_id %></div>
    <%= f.submit "Add contact", class: "btn btn-large btn-primary" %>
<% end %>